# 复制为 keystore.properties 后填写。keystore.properties 已被 .gitignore 忽略。
# storeFile 可使用绝对路径，也可使用相对于项目根目录的路径。
storeFile=C:/path/to/release.keystore
storePassword=replace-with-store-password
keyAlias=replace-with-key-alias
keyPassword=replace-with-key-password
